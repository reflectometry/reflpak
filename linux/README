NCNR Reflectometry software

Install as root:

  $ install refl* /usr/local/bin

Install as user:

  $ mv refl* ~/bin

The data subdirectory contains sample data from the
NCNR 2002 summer school.  See data/README for details.
